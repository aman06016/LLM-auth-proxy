# LLM-auth-proxy
llm-auth-proxy is a local OpenAI-compatible proxy that lets projects use standard /v1/responses and /v1/chat/completions endpoints while authenticating upstream with either Codex OAuth or a normal API key. It is designed to   smooth over compatibility differences so existing tools can work with Codex-backed models without major changes.
